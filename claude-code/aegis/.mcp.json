{
  "mcpServers": {
    "aegis": {
      "type": "sse",
      "url": "${AEGIS_MCP_URL:-http://localhost:8787/mcp/sse}",
      "headers": {
        "Authorization": "Bearer ${AEGIS_TOKEN}"
      }
    }
  }
}
